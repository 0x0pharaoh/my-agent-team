# Changelog

## Unreleased

- S0: repository scaffold, dogfooded project docs, CI matrix, adapter spikes.
- S1: daemon (loopback, HMAC-signed agents, passphrase login, CSP), tickets with claim fencing and human-only
  closing, agent seats and sessions with lineage, SSE; MCP shim; Claude Code plugin (session-start command hook,
  mcp_tool prompt hook, /my-team:init, /my-team:active, /my-team:audit); dashboard (board, agents, live status);
  `my-team install claude-code`.
